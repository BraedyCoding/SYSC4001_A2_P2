# SYSC4001_A2_P2

This is simply a multi-process (multi-question) problems, where we use fork(), exec(), shares memory and Semaphores.

The assumption I made for this was that when it said "create two new processes", I assumed that meant two child processes. 
